{
  // todo create config "pm2.scripts" which manages deamons
  "apps": [
    {"name": "message-queue"},
//    {"name": "arduino"},
    {"name": "websocket"},
    {"name": "metawear"},
//    {"name": "motion_ir"},
//    {"name": "motion_webcam"}
  ].map(function(entry) {
    entry.script     = entry.name + ".js";
    entry.out_file   = "../logs/pm-" + entry.name + ".log";
    entry.error_file = "../logs/pm-" + entry.name + ".log";
    entry.merge_logs = true;
    return entry;
  })
}
