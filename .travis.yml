language: php
php:
  - 5.5
  - 5.6
  - hhvm
  - nightly
  
before_install:
  - composer update --prefer-dist
  - echo "extension = redis.so" >> ~/.phpenv/versions/$(phpenv version-name)/etc/php.ini

script:
  - phpunit .
